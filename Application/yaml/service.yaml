kind: Service
apiVersion: v1
metadata:
# set your user name
  name: User18-hello-world
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
# set your user name
    app: User18-hello-world